---
yaml-files:
  - '*.yml'
  - '*.yaml'

rules:
  document-start: disable
  truthy:
    allowed-values: ['true', 'false', 'True', 'False', 'yes', 'no', 'on', 'off']
    check-keys: false
  comments:
    min-spaces-from-content: 2
  line-length:
    max: 120
    level: warning
